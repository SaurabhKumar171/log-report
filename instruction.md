Analyze the Apache-style access log at `/app/access.log` and create the required JSON report.

Success criteria:

1. Create `/app/report.json` as valid JSON.
2. Set `total_requests` to `6` and `unique_ips` to `3`.
3. Set `top_path` to `/index.html`.